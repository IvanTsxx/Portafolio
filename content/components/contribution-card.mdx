---
title: Contribution Card
description: Displays a single GitHub contribution item with a link, repository info and date.
featured: true
component: true
---

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<InstallCommand name="contribution-card" />

</TabsContent>

<TabsContent value="manual">

<div className="mb-0 pt-2 flex flex-col gap-4">

**1. Install the following dependencies:**

```bash
npm install framer-motion lucide-react
```

**2. Ensure you have the tooltip component installed:**

```bash
npx shadcn@latest add tooltip
```

**3. Copy and paste the following code into your project.**

<ShowSource component="contribution-card" />

**4. Update the import paths to match your project setup.**

</div>

</TabsContent>

</Tabs>

## Usage

**1. Import the component:**

```tsx
import ContributionCard from "@/registry/components/contribution-card";
```

**2. Pass the index of the contribution for delay animation:**

```tsx
const index = 0;
```

**3. Use the component:**

```tsx
const index = 0;
export function ContributionCardDemo() {
  return (
    <div className="w-full max-w-sm rounded-lg border border-border p-4 bg-background">
      <ContributionCard index={index} />
    </div>
  );
}
```
